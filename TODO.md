# Timeline — план улучшений

## В процессе
- [x] Вынести константы `SPOT_DURATION`, `SLOTS_PER_HOUR`, `START_HOUR` в `src/lib/constants.js`
- [x] Использовать константы в `CanvasTimeline.svelte`
- [ ] Использовать константы в `Timeline.svelte` (SVG-версия, сейчас неактивна)
- [ ] Убрать хардкод `* 15` из `sql/api/export.sql`

## Короткие вещи (1-2 часа)
- [ ] Проверка данных перед сохранением: `slots_save` должен отклонять пересекающиеся слоты
- [ ] Очистка осиротевших `h: true` слотов при каждом сохранении
- [ ] Триггер/функция `clean_orphan_hidden_slots()` в PostgreSQL
- [ ] Обновить `Timeline.svelte` или удалить, если больше не используется
- [ ] `.gitignore` уже добавлен — убедиться что `node_modules/` и `dist/` не попадают в коммиты

## Средние вещи (1-2 дня)
- [ ] Таблица `timeline.config(key, value)` — для `spot_duration`, `start_hour` и т.д.
- [ ] SQL-функции читают `spot_duration` из `timeline.config`
- [ ] Валидация `slots_save`: слоты идут подряд, без пробелов и дублирования
- [ ] Улучшить `groupSelectedSlots()`: проверять непрерывность, разные даты, не дублировать `rest`
- [ ] `slotsInfo` не должен сливаться со старыми данными при загрузке (`$slotsInfo = {...savedSlotsMap, ...$slotsInfo}` → заменять полностью)

## Большие вещи (3-5 дней)
- [ ] Копия системы под 20-минутные споты:
  - отдельная схема `timeline_20min` или `timeline2`
  - отдельный деплой `/timeline20`
  - отдельная `config.js` с `API_HOST`
  - SQL-функции под `timeline_20min`
- [ ] Миграции SQL (sqitch или нумерованные `001_`, `002_`)
- [ ] JSON Schema / CHECK-констрайнты на `timeline.values.value`
- [ ] Unit/e2e тесты для логики группировки и отрисовки

## Архитектурные
- [ ] Единый источник истины для конфигурации (БД + `config.js`)
- [ ] Уйти от хранения `h`/`rest`/`length` в `value` — считать их на лету из последовательности слотов
- [ ] Разделить `value` на нормализованные таблицы вместо `jsonb`
